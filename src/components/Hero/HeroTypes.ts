export type HeroComponentProps = {
  title: string,
  description: string,
  url: string,
  cta_text: string
}

export type CMSHeroProps = {
  heading: string,
  description: string,
  url: string,
  cta_text: string
}